name: figma_squircle
description: A new Flutter package project.
version: 1.0.0
homepage: https://github.com/aloisdeniel/figma_squircle

environment:
  sdk: ">=3.2.2 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  vector_math: ^2.1.4
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: