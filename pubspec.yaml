name: figma_squircle
description: Smooth corner radii for Flutter.
version: 0.6.0
homepage: https://github.com/aloisdeniel/figma_squircle

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^2.0.1
  flutter_test:
    sdk: flutter

